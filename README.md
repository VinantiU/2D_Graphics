# 2D_Graphics
#below is the prompt used for AI code genearation

#Create a menu-driven drawing application in C using  a 2D character array as the canvas.

/*Requirements:

Implement separate functions to draw a line, rectangle, triangle, and circle.
Store and manage all shapes on the canvas.
Allow users to add, delete, and modify shapes.
Redraw the canvas after every operation.
Provide a display function to print the current canvas.
Use appropriate structures/classes to store shape information and keep the code modular and well-organized.*/
